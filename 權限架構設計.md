# 權限架構設計

> **狀態**：草案 / 待討論
> **產出時機**：2026-05-09 RBAC 重構討論
> **目標**：把 hardcode 的 `policies.ts` ROLE_PERMS 搬到 DB，並在搬之前先決定組織模型 —— 因為現實是「集團代理多品牌」而非「單一品牌獨營」，組織模型直接決定權限模型。

---

## 1. 問題陳述

### 1.1 現況的兩個限制

**(a) 權限規則 hardcode**
- `src/lib/rbac/policies.ts` 的 `ROLE_PERMS` 是 TS 常數，改權限要 commit + deploy
- 角色固定 7 種（owner / manager / purchaser / warehouse / service_advisor / technician / viewer），客戶要新角色得改 code

**(b) 組織模型只有「單一品牌」**
- `profile_brands(user_id, brand_id, role)` 把 user 直接掛在 brand
- 沒有「集團」概念 → 無法表達「集團人資看所有品牌員工」「BMW 業務總監只看 BMW 不看 Mini」
- `brand_id` 是 text、沒有實體 brands 表
- `BRAND_KEY=indian` env 把整個域名綁死一個 brand，是 single-tenant 思維

### 1.2 真實場景

台灣經銷商現實：

```
汎德永業（集團）
  ├─ BMW（代理品牌）
  │    ├─ 內湖店、信義店、高雄店
  └─ Mini（代理品牌）
       └─ 信義店、台中店
```

少了任何一層都會被現實打臉：
- 「集團人資」→ 需要 group 級權限
- 「BMW 業務總監」→ 需要 brand 級權限
- 「內湖店店長」→ 需要 store 級權限

NetSuite / SAP 都用樹狀 subsidiary 解這個問題。

---

## 2. 提案的組織模型（三層樹）

### 2.1 核心 schema

```sql
-- 集團（最頂層）
groups (
  id text PRIMARY KEY,        -- 'fanho', 'huatai'
  name text NOT NULL,         -- '汎德永業'
  created_at timestamptz
)

-- 品牌（全域字典，不綁集團）
brands (
  id text PRIMARY KEY,        -- 'bmw', 'mini', 'ducati', 'indian'
  name text NOT NULL,
  manufacturer text           -- 'BMW Group'
)

-- 集團 ↔ 品牌代理權（N:N）
group_brands (
  group_id text REFERENCES groups,
  brand_id text REFERENCES brands,
  effective_from date,
  PRIMARY KEY (group_id, brand_id)
)

-- 店（最細粒度；屬於一個集團）
stores (
  id text PRIMARY KEY,        -- 'fanho_bmw_neihu'
  group_id text NOT NULL REFERENCES groups,
  name text NOT NULL,
  region text                 -- 可選
)

-- 店 ↔ 品牌（一店可複合）
store_brands (
  store_id text REFERENCES stores,
  brand_id text REFERENCES brands,
  PRIMARY KEY (store_id, brand_id)
)
```

### 2.2 關鍵設計決策

**業務表只掛 `store_id`，不掛 brand / group**

業務 row（工單、訂單、料件）只存最細粒度的 `store_id`。要算它屬於哪個 brand / group？**join 上去**。

- ✅ 不會有 brand_id 跟 store_id 矛盾的髒資料
- ✅ 加新 store 不用改業務表
- ✅ 集團架構調整只動 stores 表
- ⚠️ 每次查詢要 join（用 view 或 RLS function 包掉）

NetSuite 就是這樣 —— subsidiary 只在交易層存最細的，rollup 靠樹結構算。

### 2.3 `BRAND_KEY` env 退場

不要硬退、分階段：
- 域名綁集團（`dealeros-fanho.zeabur.app`），env 改 `GROUP_KEY=fanho`
- 進站後右上角顯示集團名 + brand/store 切換器
- 大部分 query 看 user 有權限的所有資料；少數需要 active brand 過濾的（例如展廳庫存）才用 active brand 當 filter
- RLS **不**吃 active brand，永遠照 user_assignments 算

---

## 3. RBAC 模型（兩個維度分開存）

| 維度 | 問題 | 表 |
|------|------|----|
| **能做什麼** | 「他有沒有 master.org.edit 這個權限」 | `roles`, `role_permissions` |
| **能在哪做** | 「他在哪個集團 / 品牌 / 店做」 | `user_assignments` |

**這兩個維度不要混在一張表**（避免重蹈 `profile_brands` 把 role 跟 brand 綁死的覆轍）。

### 3.1 5 張核心表

```sql
-- 1) 角色定義（取代 ROLE_PERMS hardcode）
roles (
  id text PRIMARY KEY,           -- 'manager', 'sa', 'org_admin'
  name text NOT NULL,            -- '店長', '服務顧問'
  description text,
  is_system boolean DEFAULT false  -- 系統內建 vs 客製
)

-- 2) 權限字典（permission code 仍以 ts 為 source of truth；這張表只是 metadata）
permissions (
  code text PRIMARY KEY,         -- 'master.org.edit'
  label text NOT NULL,           -- '組織架構編輯'
  module text NOT NULL,          -- 'master', 'parts', 'service'
  category text                  -- 'view' | 'edit' | 'approve'
)

-- 3) 角色 ↔ 權限（N:N，「能做什麼」）
role_permissions (
  role_id text REFERENCES roles,
  permission_code text REFERENCES permissions,
  PRIMARY KEY (role_id, permission_code)
)

-- 4) 使用者授權（核心表，「在哪做」）
user_assignments (
  id uuid PRIMARY KEY,
  user_id uuid NOT NULL REFERENCES auth.users,
  role_id text NOT NULL REFERENCES roles,
  scope_type text NOT NULL CHECK (scope_type IN ('group','brand','store')),
  scope_id text NOT NULL,        -- 對應 groups.id / brands.id / stores.id
  granted_at timestamptz DEFAULT now(),
  granted_by uuid REFERENCES auth.users,
  expires_at timestamptz         -- 可選，臨時授權
)

CREATE UNIQUE INDEX ON user_assignments (user_id, role_id, scope_type, scope_id);
```

### 3.2 一個 user 可以有多筆 assignments

```
ccc@ccc.ccc:
  ├─ assignment 1: role=manager,    scope=store:fanho_bmw_neihu
  ├─ assignment 2: role=viewer,     scope=brand:mini
  └─ assignment 3: role=org_admin,  scope=group:fanho
```

意思：在汎德 BMW 內湖店是店長、所有 Mini 店唯讀、整個汎德集團能管組織架構。

### 3.3 權限判定函數（取代 `user_has_brand`）

```sql
CREATE OR REPLACE FUNCTION user_has_permission(
  target_store_id text,
  required_perm text
) RETURNS boolean AS $$
  SELECT EXISTS (
    SELECT 1
    FROM user_assignments ua
    JOIN role_permissions rp ON rp.role_id = ua.role_id
    LEFT JOIN stores s ON s.id = target_store_id
    WHERE ua.user_id = auth.uid()
      AND rp.permission_code = required_perm
      AND (ua.expires_at IS NULL OR ua.expires_at > now())
      AND (
        (ua.scope_type = 'store' AND ua.scope_id = target_store_id) OR
        (ua.scope_type = 'group' AND ua.scope_id = s.group_id) OR
        (ua.scope_type = 'brand' AND ua.scope_id IN (
          SELECT brand_id FROM store_brands WHERE store_id = target_store_id))
      )
  );
$$ LANGUAGE sql STABLE;
```

業務表 RLS 從 `user_has_brand(brand_id)` 改成 `user_has_permission(store_id, 'parts.item.view')`。

---

## 4. Migration 路徑（三波，不要一次到位）

| Wave | 改動 | 風險 | 業務影響 |
|------|------|------|---------|
| **W1** RBAC DB 化 | 加 `roles` / `permissions` / `role_permissions`；把 `policies.ts` hardcode 灌進 DB；`profile_brands` **不動**；`hasPermission()` 介面不變 | 低 | 0（行為不變） |
| **W2** 三層組織 | 加 `groups` / `brands` / `stores` / `group_brands` / `store_brands` / `user_assignments`；`profile_brands` 跑 migration 轉成 `user_assignments`（每筆 = brand 級 assignment）；舊 `user_has_brand` 保留 compat shim | 中 | 0（資料同義轉換） |
| **W3** 業務表掛 store | 業務表逐張加 `store_id`、回填、改 RLS 用 `user_has_permission`；`BRAND_KEY` env 改 `GROUP_KEY` | 高 | 需要 demo 流程驗證、可能要客戶確認對應 |

W1 先做、W2 一週內、W3 排正式 sprint。

---

## 5. 後台 UI

| 路徑 | 內容 |
|------|------|
| `/admin/org/groups` | 集團 CRUD |
| `/admin/org/brands` | 品牌字典（系統內建為主） |
| `/admin/org/stores` | 店 CRUD（含掛 brand 多選） |
| `/admin/rbac/roles` | 角色 + 權限矩陣（左角色、上 module、勾 permission） |
| `/admin/rbac/users` | user 列表 → 點開看所有 assignments → 加 / 改 / 撤 |
| `/admin/rbac/audit` | 授權歷史（granted_by / expires_at / 撤銷時間） |

UI 規格走 `CLAUDE.md` 的 Design Pattern（List View / Page View），不要重起爐灶。

---

## 6. 待拍板的設計題

### Q1：店一定屬於一個集團嗎？
**建議**：是。複合店通常是同集團代理多 brand 共用空間 → 用 `store_brands` 多筆表達。跨集團共用空間（極少）→ 開兩個 store 處理。

### Q2：權限粒度要不要拆 CREATE / UPDATE / DELETE？
**建議**：多數模組保留 VIEW / EDIT 兩段；少數真有業務分流的單獨拆（例如 PR/PO 已經拆出 `_CREATE` / `_APPROVE`）。
- 真要拆，建議拆 `*.delete` 獨立（刪除真的會出事），`*.create` 跟 `*.update` 合併為 `*.edit`。
- 新增 vs 修改業務上很少分權，拆了徒增 UI 複雜度。

### Q3：role 是否分集團 / 品牌？
例如「汎德的 manager」跟「華泰的 manager」是同一個 role 還是兩個？

**建議**：role 全站共用（`roles` 表沒有 group_id 欄位），透過 `user_assignments.scope_*` 區分是哪個集團/品牌/店的 manager。這樣 UI 上 role 只有一份清單，不會炸開。

### Q4：要不要支援 role 繼承？
例如「店長 inherits 業務員 + 倉管」。

**建議**：先**不**做。複雜度爆炸、debug 痛苦；改用「同一個 user 給多筆 assignments」表達。等真有客戶要求再說。

---

## 7. 對應到 ccc@ccc.ccc 這個 case

現況：`profile_brands(ccc, 'ducati', 'member')` —— `member` 不在 ROLE_PERMS、`ducati` 跟訪問的 indian 站不符。

W1 完成後：
```sql
-- 移除舊 row
DELETE FROM profile_brands WHERE user_id = ccc;

-- 給 indian 集團 manager 權限（假設 indian 屬於某集團 'iat'）
INSERT INTO user_assignments (user_id, role_id, scope_type, scope_id)
VALUES (ccc, 'manager', 'brand', 'indian');
```

立刻拿到 indian 全部品牌的所有店的 manager 權限。

---

## 8. 下一步

選一個起點：
1. **跑 W1 schema migration** + 移植 hardcode → 立刻拿到「DB 控管 role」的能力
2. **畫 W2 ER 圖** + 詳細 migration plan（含 profile_brands 轉換 SQL）→ 想清楚再動 schema
3. **先做 `/admin/rbac/roles` UI mockup** → 確認操作流再接 DB

建議順序：**2 → 1 → 3**。
