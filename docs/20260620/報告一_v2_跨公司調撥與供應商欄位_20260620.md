# 報告一 v2：跨公司調撥記帳（結論不變）+ 供應商欄位落地

**日期：2026-06-20　｜　Partner AI Agent → Russell Hung**
**對應：6/19 裁示 §一 + §三　｜　承 v1**

---

## 一、跨公司調撥記帳（§一）— 結論不變：☑ 已分離

v1 結論維持：調撥成本資料**已經把「事實」與「會計判斷」分開**，與保固理賠同一套設計。

- `stock_transfers` / `stock_transfer_lines` / `vehicle_transfers` 三表**零會計欄位**（無 account_id / journal_entry_id / debit / credit / posted）。
- `unit_cost` 是調撥當下的移動平均成本**快照（事實）**，任何外部會計系統都能直接讀用。
- 會計判斷（同/跨 subsidiary）刻意留在 `receiveTransfer()` 動作層、用「倉→組織→法人」執行期推導，**不寫進資料**；跨 subsidiary 只留標記不入帳。

**本輪（work_orders 修復）未涉及調撥模組，結論與 v1 完全一致，無需補件。** 唯一的未來銜接點：接會計系統時在 `stock_receipts` 加 `journal_entry_id` 回指欄（仍為事實欄）。完整證據見 v1 報告。

---

## 二、供應商「原廠經銷商代碼」（§三）— 已落地

裁示「依專案慣例先存入 metadata、不開 typed column」**已照做**。

- **資料層**：`suppliers` 表早已有 `metadata jsonb`；原廠經銷商代碼存 `metadata.oem_dealer_code`（DB 中 `OEM-POLARIS` 那筆原本就有此 key，現在 UI 接上了）。**未新增 typed column**，符合裁示。
- **Domain helper**（`src/domain/suppliers.ts`）：照既有 `supply_categories` 的 metadata 寫入 pattern，新增 `oem_dealer_code` 欄位——`SupplierWriteInput` 加欄、`buildSupplierWritePatch` 的 metadata merge block 加邏輯（空字串即移除 key）、`listSuppliersWithContract` 與 `updateSupplier` 同步處理。merge-on-write，不覆蓋 metadata 其他 key。
- **UI**（`/parts/setup/suppliers/[id]` detail view）：基本資料區塊（統一編號旁）新增「原廠經銷商代碼」欄位，view/edit/create 三模式皆可顯示與編輯。
- **驗證**：tsc / eslint / 天條 audit（UI 不直連 supabase）全綠。

---

## 三、Indian Dealer Portal 串接（§四）

三階段方案方向確認，本輪無需動作。

---

*DealerOS 機密文件　｜　Partner AI Agent　｜　2026-06-20 v2*
