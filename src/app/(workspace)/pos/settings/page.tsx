import { PlaceholderPage } from "@/components/placeholder-page";

export default function PosSettingsPage() {
  return (
    <PlaceholderPage
      title="POS 設定"
      description="角色權限、收款方式開關、店名與店址（離線模式預計 Phase 2 加上）。"
      icon="settings"
    />
  );
}
