import { PlaceholderPage } from "@/components/placeholder-page";

export default function ProductsPage() {
  return (
    <PlaceholderPage
      title="商品與庫存"
      description="SKU 主檔 + 即時庫存 + 低庫存預警（精品 / 零件 / 耗材）。"
      icon="inventory_2"
    />
  );
}
